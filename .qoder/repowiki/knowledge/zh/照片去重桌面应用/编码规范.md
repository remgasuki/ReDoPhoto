- 主进程服务函数普遍采用异步批处理模式，并在批次间使用 `setImmediate` 让出事件循环，以防止长时间计算阻塞 UI 响应。
- 前后端通过预加载脚本中定义的强类型接口 (`FileInfo`, `DuplicateGroup` 等) 保持数据结构一致，确保 IPC 通信的类型安全。
- 渲染进程组件遵循基于状态机相位 (`phase`) 的条件渲染模式，根据 `scanStore` 中的当前阶段动态切换显示不同的步骤组件。